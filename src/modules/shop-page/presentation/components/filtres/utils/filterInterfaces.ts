export interface Price {
    id: number,
    min: number | null,
    max: number | null
}

export interface Category {
    id: number,
    title: string
}

export interface Sort {
    id: number,
    title: string
}